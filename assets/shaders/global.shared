// global.h
#define AMBIENT_STRENGTH 0.f

struct PointLight {
	vec4 position;
	vec4 color;
};

layout(set = 0, binding = 0) uniform SceneData {
	mat4 view;
	mat4 projection;
	PointLight light;
	vec3 camera_position;
	float _pad0;
}
u_scene;
