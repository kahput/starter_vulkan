#version 450
#pragma shader_stage(fragment)

#include "spatial.glsl"

in ShaderInput {
    layout (location = 0) vec3 worldspace;
} fs_input;

layout(location = 0) out vec4 out_color;

void main() {
    out_color = texture(u_skybox, fs_input.worldspace); 
}
